export default interface ServerInfo {
    id: number,
    name: string,
    status: string,
    activePlayers: string,
    totalPlayers: string,
    version: string,
    ip: string,
    port: string,
    icon: string,
}
